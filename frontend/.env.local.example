# Copy to .env.local for local development. Never commit real secrets here —
# this is a public, browser-exposed value (NEXT_PUBLIC_*), not a place for
# credentials. The backend itself holds all real secrets (DB/MLflow/etc).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8123
