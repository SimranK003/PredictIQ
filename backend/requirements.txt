# Runtime dependencies only — installed into every image (backend,
# worker, migrate) that actually runs the application. Test/lint
# tooling lives in requirements-dev.txt instead (see that file's
# header) so pytest/ruff never ship inside the production image (Phase
# 10 hardening — a stray dev dependency's CVE shouldn't count against
# the deployed container, and it's dead weight in image size either
# way). `pip install -r requirements-dev.txt` pulls this file in too.

# --- Web / API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.32

# --- Database ---
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.9

# --- Async jobs ---
celery==5.4.0
redis==5.0.8

# --- ML ---
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.2
xgboost==2.1.1
mlflow==2.16.2
joblib==1.4.2
scipy==1.14.1
pyyaml==6.0.2

# --- Monitoring ---
prometheus-client==0.20.0

# --- Auth ---
argon2-cffi==25.1.0

# --- Object storage (Phase 10 production dataset storage; see
# app/services/dataset_storage.py). Only imported when
# DATASET_STORAGE_BACKEND=s3 — native/Docker Compose dev never touches
# this code path, but it's a real dependency of the image either way. ---
boto3==1.43.98

# --- Utilities ---
python-json-logger==2.0.7
httpx==0.27.2
