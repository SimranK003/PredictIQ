# Local development / CI only — never installed into a runtime image
# (backend/worker/migrate all install requirements.txt alone; see
# backend/Dockerfile). Install with:
#   pip install -r requirements-dev.txt

-r requirements.txt

pytest==8.3.3
pytest-cov==5.0.0
pytest-env==1.1.5
ruff==0.6.9
pip-audit==2.10.1
